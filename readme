This project is an interactive sticky note board that can be used by small teams or groups,
to keep some liveliness to boring reminders and priority lists.

The backend is coded with Java using Spring Boot framework : 80% done, need database
The frontend will be coded in Javascript using react, to consume the user input and send to backend to process: 20% done, learning React
